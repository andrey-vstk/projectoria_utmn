# Shared
NODE_ENV=production
APP_URL=https://проектория.site
API_URL=https://проектория.site/api

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/projectoria?schema=public

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Auth
JWT_ACCESS_SECRET=change_me_access_secret
JWT_ACCESS_TTL=8h
JWT_COOKIE_NAME=projectoria_access
CSRF_COOKIE_NAME=projectoria_csrf

# Admin bootstrap
SEED_ADMIN_EMAIL=admin@utmn.local
SEED_ADMIN_PASSWORD=admin12345
SEED_ADMIN_NAME=Администратор

# SMTP
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Проектория <your-email@example.com>"

# LLM
# mock | external | n8n
LLM_PROVIDER=mock
LLM_API_URL=
LLM_API_KEY=
LLM_MODEL=

# n8n integration
N8N_WEBHOOK_URL=
N8N_EMAIL_WORKFLOW_URL=
# example: http://localhost:5678/webhook/llm-analyze
# Docker Compose: http://n8n:5678/webhook/llm-analyze
# Local (без Docker): http://localhost:5678/webhook/llm-analyze
N8N_LLM_WEBHOOK_URL=
N8N_LLM_TIMEOUT_MS=900000

# Public links
PUBLIC_BASE_URL=https://проектория.site

# Web
NEXT_PUBLIC_API_URL=/api

# Reverse proxy
CADDY_DOMAIN=xn--e1agfoccheq7k.site
CADDY_EMAIL=admin@example.com

# Rate limit
THROTTLE_TTL=60
THROTTLE_LIMIT=20
